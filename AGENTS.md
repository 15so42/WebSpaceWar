# 开发准则

必须使用 UTF-8 编码。

你正在开发一款真正可玩的多人联机 2D 像素风太空卡牌 RTS，不是静态演示页面。

## 数据与模块

- 飞船属性、卡牌费用、占领速度、采矿收益、特殊星球效果、Debuff 和胜利条件必须数据化。
- 不要把数值散落在 UI、渲染逻辑或网络消息处理代码中。
- 将星球、飞船、战斗、占领、资源、卡牌、输入、UI 和网络同步拆分为职责清晰的模块。
- UI 与表现层只能读取状态，不能直接修改游戏规则状态。
- 不要将核心规则堆入单一主循环、UI 文件或 WebSocket 回调。

## 多人联机架构

采用 Host 权威架构：

- Host 是唯一执行游戏规则的节点。
- Client 只能上传操作命令和 UI 输入。
- Host 本地玩家也必须通过与 Client 相同的命令入口、验证和结算流程。
- Client 不得自行结算资源、抽牌、建造、伤害、护盾、占领、采矿、间谍、Debuff、卡牌效果或胜负。
- Client 只渲染 Host 的权威状态。
- 中继服务器只负责房间、身份、心跳、目标路由和不透明载荷转发，不得裁定游戏规则。

所有舰队派遣命令必须来自：

源星球拖动 → 目标星球 → 环形 UI 明确确认

Host 必须验证：

- 玩家身份与阵营归属。
- 来源星球、目标星球、飞船与卡牌是否归该玩家所有。
- 飞船类型是否允许对应操作。
- 资源、费用、冷却、目标、游戏阶段和占领状态是否合法。
- 命令是否重复、伪造或过期。

## 多玩家模型

- 使用动态 playerId、factionId、planetId、shipId、effectId、cardDefinitionId、cardInstanceId 和 commandId。
- 不得写死 p1、p2、固定两个阵营、固定基地数量或固定玩家人数。
- 不得使用数组下标作为网络状态、卡牌或效果的稳定身份。
- 默认支持多人自由混战；组队、PvE 与 AI 阵营通过 MatchRules 扩展。

## 同步、断线与版本

- 持久状态只在变化时发送补丁，不做固定频率全量同步。
- 飞船位置和动画可高频同步，但只能服务于平滑表现，不能承载规则判定。
- 开局、加入、重连和纠错时，Client 从 Host 当前完整快照重建状态，不重放旧事件。
- 恢复完成前，Client 禁止提交新命令。
- Client 断线后不得自动重连，必须询问玩家是否恢复对局。
- Host 断线后房间保留 60 秒；期限内恢复可继续，超时释放房间。
- 不做 Host 迁移，不伪造丢失的权威状态。
- 游戏版本、网络协议版本、卡牌与静态数据目录版本必须完全一致；任一不匹配或缺失时，拒绝加入、开始和恢复。

## 性能与验证

- 大量飞船存在时，避免每帧全量两两扫描；使用空间划分、邻域查询或按星球分组。
- 复用飞船、特效和临时对象，避免频繁创建销毁。
- 修改玩法后，必须进行对应规则验证。
- 每次交付前执行静态检查、构建检查，并尽可能验证 Host 与 Client 的实际流程。
- 只修改当前任务相关文件，不覆盖、清理或回滚用户未提交的改动。
- 未经明确授权，不执行 git commit、git push、创建 Pull Request 或部署公网。
